import Container from "react-bootstrap/esm/Container";

const NotFound = (props) => {
	return (
		<>
			<Container fluid>
				<h1>NotFound 😕</h1>
			</Container>
		</>
	);
};

export default NotFound;
